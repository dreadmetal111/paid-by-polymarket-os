[Unit]
Description=House of Markets Docker ingress firewall
Requires=docker.service
After=docker.service tailscaled.service
PartOf=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/pbp-docker-firewall.sh
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target docker.service
